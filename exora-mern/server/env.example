# Copy this to .env and adjust for your local PostgreSQL
PORT=5000

# Option A: Individual settings
PGHOST=127.0.0.1
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password
PGDATABASE=exora-web

# Option B: Single connection string (overrides above if set)
# DATABASE_URL=postgres://postgres:your_password@127.0.0.1:5432/exora-web

